server:
  port: 0
spring:
  application:
    name: API-GATEWAY
  cloud:
    discovery:
      enabled: false
    gateway:
      enabled: false
  zipkin:
    enabled: false
eureka:
  client:
    enabled: false