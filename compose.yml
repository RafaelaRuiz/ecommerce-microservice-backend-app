services:
  zipkin-container:
    image: openzipkin/zipkin
    ports:
      - 9411:9411
  service-discovery-container:
    build:
      context: ./service-discovery
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/service-discovery-ecommerce-boot:0.1.0
    ports:
      - 8761:8761
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  cloud-config-container:
    build:
      context: ./cloud-config
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/cloud-config-ecommerce-boot:0.1.0
    ports:
      - 9296:9296
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  api-gateway-container:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/api-gateway-ecommerce-boot:0.1.0
    ports:
      - 8080:8080
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  order-service-container:
    build:
      context: ./order-service
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/order-service-ecommerce-boot:0.1.0
    ports:
      - 8300:8300
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  product-service-container:
    build:
      context: ./product-service
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/product-service-ecommerce-boot:0.1.0
    ports:
      - 8500:8500
    environment:
      - SPRING_PROFILES_ACTIVE=dev
  user-service-container:
    build:
      context: ./user-service
      dockerfile: Dockerfile
    image: ${DOCKERHUB_USERNAME}/user-service-ecommerce-boot:0.1.0
    ports:
      - 8700:8700
    environment:
      - SPRING_PROFILES_ACTIVE=dev
